# PodDar Config

# Options
dryrun: true

# PodConfigs
configs:
  path: test/configs
  pods: [pod1, pod2]
  groups:
    group1: [pod1, pod3, pod4]
    group2: [pod2, pod3, pod5, pod42]

# Pods
pods:
  path: /pods
